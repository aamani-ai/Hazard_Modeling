# ── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg

# ── Environments ─────────────────────────────────────────
.venv/
venv/
env/
ENV/
.python-version

# Local secrets — never commit
.env
.env.*

# ── Jupyter ──────────────────────────────────────────────
.ipynb_checkpoints/
profile_default/
ipython_config.py

# ── Testing / coverage ───────────────────────────────────
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.cache/

# ── Data / model artifacts (keep folder structure, ignore big files) ──
*.parquet
*.h5
*.hdf5
*.nc
*.grib2
*.grib
*.tif
*.tiff
*.idx
*.gz
*.pkl
*.pickle
*.joblib
local_data/
local_docs/

# Raw hazard-data cache (re-fetchable rasters + raw API dumps) — keep summaries/manifests, not the raw pulls
data/*/raw/

# ── IDE ──────────────────────────────────────────────────
.idea/
.cursor/
.vscode/
*.swp
*.swo
*~

# ── OS ───────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db

# ── Claude / agent workspace ─────────────────────────────
.claude/

# ── Local cross-project nav symlinks ─────────────────────
#    (machine-specific absolute paths — must NOT be committed to the shared repo)
/Learning
/model-gpr
/hazard_analysis
/infrasure-hazard-competitive-research
/renewablesinfo_org
/infrasure-damage-curves

# Temporary mine of the old wildfire "value-farm" model — extract base case + data sources +
# domain info into Notebooks/wildfire/, then DELETE this symlink. Not a permanent reference.
/_legacy_wildfire

# Heavy / proprietary local reference (internal solar risk assessment) — public repo, keep local only
docs/extra/kWh_Solar_Risk_Assessment.2026.pdf
